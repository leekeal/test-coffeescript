// Generated by CoffeeScript 1.6.3
(function() {
  var formidable, fs, queryString;

  queryString = require("querystring");

  fs = require("fs");

  formidable = require("formidable");

  /*　開始の処理する関数*/


  exports.start = function(response) {
    var body;
    console.log("start was called");
    body = '<html>' + '<head>' + '<meta http-equiv="Content-Type" content="text/html; ' + 'charset=UTF-8" />' + '</head>' + '<body>' + '<form action="/upload" method="post" enctype="multipart/form-data">' + '<input type="file" name="upload">' + '<input type="submit" value="Upload file" />' + '</form>' + '</body>' + '</html>';
    response.writeHead(200, {
      "Content-Type": "text/html"
    });
    response.write(body);
    return response.end();
  };

  /*　アップロードしたデータを処理する関数*/


  exports.upload = function(response, request) {
    var form;
    console.log("Request handler 'upload' was called.");
    form = new formidable.IncomingForm();
    form.parse(request, function(error, fields, files) {
      return fs.renameSync(files.upload.path, "upload/test.png");
    });
    response.writeHead(200, {
      "Content-Type": "text/html"
    });
    response.write("received image:<br/>");
    response.write("<img src='/show' />");
    return response.end();
  };

  exports.show = function(response) {
    console.log("Request handler 'show' was called");
    return fs.readFile("upload/test.png", "binary", function(error, file) {
      if (error) {
        response.writeHead(500, {
          "Content-Type": "text/plain"
        });
        response.write(error + "\n");
        return response.end();
      } else {
        response.writeHead(200, {
          "Content-Type": "image/png"
        });
        response.write(file, "binary");
        return response.end();
      }
    });
  };

}).call(this);
